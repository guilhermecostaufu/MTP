#include <stdio.h>
void preencher(float **matriz, int )
int main()
{
    float **matriz;
    matriz=(float**)calloc(N,sizeof(float*));
    for(i=0;i<N;i++)
        matriz[i]=(float*)calloc(M,sizeof(float));
    return 0;
}
